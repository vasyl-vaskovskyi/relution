// demo (local compose): production build with debug output on; localStorage['appstore.debug']
// may override it at runtime (docs/architecture/frontend.md#debug-logging)
export const environment = {
  debugLogging: true,
  debugLoggingOverride: true,
};
