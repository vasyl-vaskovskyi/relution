package com.example.appstore.catalog;

/** Our kind vocabulary. Extensible: clients must tolerate new values ({@code docs/api/README.md}). */
public enum AppKind {
    IOS_APP,
    MAC_APP,
    OTHER
}
