package com.example.appstore.catalog;

/** Device families an app runs on. Extensible: clients must tolerate new values ({@code docs/api/README.md}). */
public enum DeviceFamily {
    IPHONE,
    IPAD,
    IPOD,
    MAC,
    WATCH,
    TV
}
